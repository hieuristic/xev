#pragma once
#include <cstdint>

namespace xev {
namespace fs {

constexpr uint64_t EOF_COUNT = static_cast<uint64_t>(-1);
constexpr uint32_t INVALID_MOUNT = static_cast<uint32_t>(-1);

}  // namespace fs
}  // namespace xev
